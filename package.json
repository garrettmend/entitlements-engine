{
  "name": "entitlements-engine-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests that drive index.html against the live entitlements-engine API",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
